import { WithinDurationPipe } from './within-duration.pipe';

describe('WithinDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new WithinDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
